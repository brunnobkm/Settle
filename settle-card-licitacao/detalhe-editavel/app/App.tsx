// Detalhe do card (editável): o protótipo original era idêntico ao card de licitação,
// então esta página reaproveita a mesma tela (../../app).

import { PaginaDoCard } from "../../app/App"

export default function App() {
  return <PaginaDoCard titulo="Detalhe do card (editável)" />
}
